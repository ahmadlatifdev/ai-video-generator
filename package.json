{
  "name": "ai-video-generator",
  "version": "1.0.0",
  "description": "AI-powered video generator using Node.js",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.7.0",                       // Upgraded for security
    "express": "^4.18.2",                    // Existing web framework
    "openai": "^4.0.0",                      // Existing AI integration
    "fluent-ffmpeg": "^2.1.2",               // Video processing
    "@ffmpeg-installer/ffmpeg": "^1.1.0",    // FFmpeg binaries (corrected typo from "effmpeg")
    "fs-extra": "^11.2.0"                    // Enhanced file system operations
  },
  "engines": {
    "node": ">=18.0.0"                       // Matches your Dockerfile
  }
}
